<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <application
        android:label="Bebop"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bebop"
        android:usesCleartextTraffic="true">

        <!--
            Paysage forcé : pilotage à deux pouces sur une vidéo 16:9.
            sensorLandscape (et pas landscape) pour que le téléphone puisse être
            retourné dans l'autre sens selon le côté où sort le câble.
            configChanges évite que la rotation recrée l'activité, ce qui
            couperait la liaison au drone en plein vol.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Bebop"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.Bebop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED" />
            </intent-filter>

            <meta-data
                android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED"
                android:resource="@xml/accessory_filter" />
        </activity>
    </application>

</manifest>
